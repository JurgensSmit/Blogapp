<h3>Comments</h3>
<% @post.comments.each do |comment| %>
	<h4><%= comment.commenter %></h4>
	<p><%= comment.body %></p>
	<%= link_to "Delete commment", [comment.post, comment], method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>